# 術語對照表

文件一律中文，但**專有名詞第一次在該文件出現時**，於其後加括號標註英文原文 ——
規則見 `CLAUDE.md` 的〈文件與 commit 的語言慣例〉。

**這張表是唯一來源。** 表裡沒有的詞，先加進表再用，不要自己另譯。

| 記號 | 意思 |
|---|---|
| （無） | 標準用語：有規範、辭典或官方文件依據 |
| △ | 常見用法：業界通行但無單一定譯 |
| ⚠ | 沒把握：查不到定譯，**用之前請先確認** |

⚠ **逐字轉錄的區塊不准加註**：規格 §5 的迴圈 code block、§7.3 的 system prompt、
§10 的參數區塊、§6.1 的 plan 範例。`tests/smoke.py` 的 `[9]` 會擋。


## 營建製圖（63）

台灣營建與製圖實務用語。**這一組最容易標錯** —— 字面直譯多半是錯的。

| | 中文 | 英文 | 對應識別碼 | 備註 |
|---|---|---|---|---|
| ★ | 圖框 | drawing frame | `frame / SHEETS.frame_wkt / EXCLUDE_KINDS / drawing_frame` | 國教院：圖框→border{CAD}（土木工程名詞）、border（機械工程名詞）。ISO 5457 同時用 frame 與 borders 兩詞；美式圖面常說 border。取 drawing frame 為主詞是為了與程式端字根 frame 一致，備選 border 放 note。第一個可標位置：§3 第 140–141 行。 |
| △★ | 尺寸鏈 | dimension chain | `chain / 05_chains.csv / 05_chain_members.csv / EVIDENCE_NS 的 chain#` | ★ 四組有衝突，取兩票的 dimension chain 為主詞，理由是它與程式端字根 chain 一致（chains.closed、chain_closure、CHAIN_MEMBERS）。放棄的譯法：dimensional chain（第 1 組提案，國教院機械工程／兩岸對照確有此詞條，但那是公差鏈分析，語意不同）、dimension string（美式事務所口語，可… |
| ★ | 層高 | floor-to-floor height | `config.ASSUMED 層高cm_1F / 層高cm_其他` | 法源明確：建築技術規則建築設計施工編第 1 條第 13 款「樓層高度：自室內地板面至其直上層地板面之高度」＝ floor-to-floor height。★ 絕不可譯 ceiling height／clear height／floor-to-ceiling height——同條第 14 款另定「天花板高度」＝ceiling height（＝淨高，等於層高減板厚與樑深），在… |
| ★ | 平面圖 | floor plan | `02_sheets 的 kind == "plan"` | ⚠ 與「繪圖計畫（drawing plan，程式裡的 plan）」撞字，而且兩者在同一份文件都會大量出現——第一次標註時務必把兩者分開說明，否則讀者會把 plan_vN.yaml 當成平面圖。 |
| △★ | 建照 | construction license | `cases/68-01782（案號前綴即民國 68 年建照）` | ★ 兩組衝突，取有法源的一方。已查 law.moj.gov.tw《建築法》(Building Act) 英譯第 28 條：建造執照／雜項執照／使用執照／拆除執照 官方英譯為 construction license／miscellaneous license／usage license／demolition license，全文以 license 為主。「建照」＝建造執照… |
| ★ | 曬圖 | diazo print |  | ★ CLAUDE.md §語言慣例已把「曬圖（diazo print）」釘死為示範用例（第 145 行），照用、不要自己另譯。1979 年台灣建照圖的複製是氨薰重氮法（whiteprint／blue-line process），白底藍紫線；藍底白線的 cyanotype 才是嚴格意義的 blueprint。放棄的譯法：blueprint（口語泛稱，會與 cyanotype… |
| ★ | 標高 | elevation | `level_line / level_text` | 國教院（計量學、測繪學辭典）與 IFC 雙重有據：IfcBuildingStorey.Elevation 定義為「Elevation of the base of this storey, relative to the 0,00 internal reference height」，與圖上 ±0.00／+3.20 語意一致。★ 陷阱：同段的「立面圖」也是 elevati… |
| ★ | 讀數 | reading | `04_readings.csv / fields.READINGS / 工具 read_number` | 國教院多領域一致（物理學、力學、氣象學、天文學、核能名詞）：讀數→reading，初始讀數 initial reading、零讀數 zero reading。量測領域的儀器讀數就是 reading，程式端已用 readings，一致無爭議。 |
| △ | 1/2B 磚牆 | half-brick wall | `classes.py material_note 詞庫 / config.ASSUMED 牆厚cm_內: 12` | 同 1B 體系。英規 half-brick wall≈102.5mm、台灣≈12cm。原文常只寫「1/2B」沒寫「磚牆」，建議標「1/2B（half-brick wall，約 12cm）」。降級理由同 1B。⚠ 曬圖擴散後 12cm 內牆的線對常糊成單一粗帶（classes.py wall 判準），這是它與 1B 在偵測上分不開的原因。 |
| △ | 1B 磚牆 | one-brick wall | `classes.py material_note 詞庫 / config.ASSUMED 牆厚cm_外: 24` | 英式砌磚以「磚長」計厚：B 指 brick（磚長），不是等級——★ 絕不可譯 first-class brick／B-type brick。英規 one-brick wall≈215mm，台灣 1B≈24cm（紅磚 23×11×6 加灰縫與粉刷），與 ASSUMED 相符。★ 降為常見用法：國教院查無「1B」詞條，且「1B」是台日製圖慣例、英美不寫，英文讀者未必認得，建議… |
| △ | 一點鏈線 | long-dashed dotted line |  | 即點劃線。CNS／JIS 稱一點鎖線，ISO 128 現行寫法是 long-dashed dotted line，BS 舊稱 chain line，美式常直接叫 center line。出處：classes.py EXCLUDED 軸線條目「一點鏈線的長短節奏…曬圖後復原不了」。⚠ 別把 chain line 與「尺寸鏈 dimension chain」的 chain 混… |
| ⚠ | 二丁掛 | nichōgake tile | `classes.py material_note 構造詞庫` | ★ 已修正拼寫：日語「二丁掛け」讀作 にちょうがけ＝nichōgake；原提案的 nichoumagake 是錯的。ja.wikipedia 的〈煉瓦〉〈タイル〉兩條目都未收這個規格名，讀音與尺寸（約 60×227mm）皆無法佐證。英文文獻多用描述式 brick-look ceramic facing tile。⚠ 只在詞庫列舉出現時建議不標。 |
| △ | 側面圖 | side elevation |  | 國教院把 side elevation／end elevation 對到「側視圖，側立面圖」（工程圖學）；但「側面圖」本身在國教院對到的是 lateral plan／profile／side view，偏機械與船舶製圖。本案是建築圖紙上與四個立面圖並列的一張，取建築慣用的 side elevation，故只算常見用法。 |
| △ | 元件編號 | element tag | `element_tag（classes.py）` | W1／D2／C1／窗1／門2。英美製圖慣稱 mark（door mark、window mark），BIM 界慣稱 tag；本專案識別碼取 tag，故 element tag 為主詞、mark 放 note。 |
| △ | 剖切符號 | section mark | `section_mark（classes.py EXCLUDED）` | 英文亦作 section tag／section callout／section cut symbol。 |
|  | 剖面圖 | section drawing | `kind == "section"` | classes.py hatch 判準「等距同角度的短平行線群只在剖面圖與大比例詳圖成立」。 |
| △ | 剖面填充 | hatch | `hatch（classes.py）` | 1:100 手繪實務的主分支是整段塗黑（solid fill；建築語彙稱 poché），等距斜線群（material hatching）是第二分支，只在剖面與大比例詳圖成立——classes.py 判準刻意把兩種寫法分開。中文亦說「塗黑／材質剖線」。 |
| △ | 加強磚造 | confined masonry | `classes.py material_note 構造詞庫` | confined masonry 是耐震工程的正式分類（Eurocode 6 與 unreinforced／reinforced／prestressed 並列），語意（磚牆先砌、四周再澆置 RC 柱梁圍束）確實對應。★ 但台灣官方英譯本次無法核對（法規英文頁抓不到、WebSearch 額度用罄），故降為常見用法。台灣文件另見 reinforced brick masonr… |
| △ | 印章區 | stamp area | `stamp（EXCLUDE_KINDS）` | ★ 主從已調整：程式端釘死 stamp，故 stamp 放前面，放棄以 seal 為主詞。台灣建照圖上是建築師、起造人、主管機關的用印區；正式文件說 architect's seal，口語 stamp。「區」用 area 較安全（block 會與 fields.QUALITY 的區塊混淆）。第一個可標位置：§3 第 141 行 02_exclude 說明。 |
| △ | 圖種 | drawing type | `kind（02_sheets.csv 的 kind 欄，值如 plan／elevation／section）` | 國教院查無「圖種」詞條。指平面／立面／剖面／詳圖這層分類，由 s02_layout 一次 VLM 呼叫讀出。建議首次標成「圖種（drawing type；02_sheets 的 kind 欄）」，讀者才接得上程式端。第一個可標位置：§3 第 140 行。 |
|  | 地界線 | property line | `classes.py EXCLUDED slab 條目` | 美式製圖慣稱 property line／lot line；地籍脈絡稱 cadastral boundary。與板緣同形，規則式與人眼都分不開，是排除 slab 的理由之一。 |
|  | 垂直載重路徑 | vertical load path | `classes.py column 立論` | 結構工程標準詞（亦作 gravity load path）。「柱是垂直載重路徑的起點，也是跨樓層對位最可靠的錨」。 |
|  | 基礎 | foundation | `foundation（classes.py EXCLUDED）` | 「建照 A 系列沒有基礎平面圖（在 S 系列）」。 |
| △ | 天井 | light well |  | 台灣用法指採光通風用的內凹開放空間。classes.py room_label 立論「判定封閉牆圍區域是室內而非天井」。★ 絕不是 ceiling（「天」字陷阱）；也不完全等於 courtyard（後者較大、常是庭院）。英文亦見 air well。 |
|  | 女兒牆 | parapet |  | 屋頂周邊的矮牆，英文定譯 parapet（wall），無疑義。出處：classes.py EXCLUDED 的 slab 條目——女兒牆投影線與板緣同形，是排除 slab 類的理由之一。 |
| ⚠ | 子圖 | drawing view | `02_sheets.csv（一行一張子圖）` | 指同一頁上的每一張圖（A-2 一頁六張）。英文沒有單一定譯：BIM／CAD 慣例最接近 view（drawing view），drawing／sub-drawing 也通。★ 需負責人選定後寫進對照表。⚠ 與 sheet（圖幅／一張圖紙）不可互換：02_sheets 的一列＝一張子圖，這個命名本身就有張力。 |
|  | 子圖圖名 | drawing title | `drawing_title（classes.py EXCLUDED）` | 圖名是 s02_layout 的輸入，不發偵測 id。 |
| △ | 室名 | room label | `room_label（classes.py）` | ⚠ 語意分工：內容叫 room name，圖上那串字叫 room label／room tag。本專案識別碼是 room_label，故以 room label 為主詞。詞庫含店鋪／客廳／臥室／廚房／浴廁／樓梯間／車庫／陽台／騎樓。 |
|  | 尺寸數字 | dimension text | `dim_text` | 製圖文獻亦作 dimension value／dimension figure，但識別碼是 *_text，標 dimension text 才與 level_text 成套。classes.py 命名規約：*_line 一律是幾何／拓樸，*_text 一律是要走三源進 04_readings 的值。 |
|  | 尺寸標註 | dimensioning |  | 指整組標註的行為與成果（尺寸線＋尺寸界線＋數字）。出處 perception/s03_texts.py「不是只挑尺寸標註」。 |
|  | 尺寸界線 | extension line | `dim_witness（classes.py EXCLUDED）` | ASME Y14 稱 extension line，BS／ISO 稱 projection line 或 witness line。識別碼取 witness，故建議首次標成「尺寸界線（extension line／witness line）」。 |
|  | 尺寸線 | dimension line | `dim_line` | core/classes.py 的正式類別名，該檔註明「§8 逐字指名，名字一字不可改」。英文定譯無疑義。 |
|  | 引線 | leader | `leader（classes.py EXCLUDED）` | 從註記拉向構件的細線。classes.py material_note 判準寫明「引線併入本筆當 evidence，不另立類」。完整說法 leader line。 |
|  | 指北針 | north arrow | `north_arrow（classes.py EXCLUDED）` | classes.py 認為它將來會變成 02_sheets 的一個欄位，故不發永久偵測 id。 |
| ⚠ | 挑高 | increased floor-to-floor height | `config.ASSUMED 層高cm_1F: 400 # 店鋪常挑高` | ★ 兩組結論衝突，取語意正確的一方。國教院查無建築義的「挑高」（只有電子工程的 height-increased pillar、體育的挑高球），這是台灣房地產／建築口語，英文沒有單一對等詞：空間真的兩層通高＝double-height；只是層高加大＝increased floor-to-floor height。本專案指 1F 店鋪 400cm（對照其他層 320cm），… |
| △ | 材料註記 | material note | `material_note（classes.py）` | 亦說「構造註記」。詞庫為 RC／R.C.／磚／1B／1/2B／加強磚造／洗石子／二丁掛／粉刷／cm，是 thickness_src 脫離 assumed 的第二條實據來源。英文亦可寫 material/construction annotation。 |
|  | 梁 | beam | `beam（classes.py EXCLUDED）` | classes.py 指出建照 A 系列建築圖本來就不畫梁（在 S 系列結構圖上），故 v1 不收。A 系列 = A-series（建築圖）、S 系列 = S-series（結構圖）。 |
|  | 樓板 | slab | `slab / slab_edge / roof（classes.py EXCLUDED）` | 完整寫法 floor slab。classes.py 建議日後 append 時用 slab（十年後承載 IfcSlab）而非製圖視角的 slab_edge（板緣）。板厚 = slab thickness（config.ASSUMED 板厚cm: 15）。 |
|  | 標題欄 | title block | `title_block / SHEETS.title_block_wkt` | 國教院（工程圖學、機械工程名詞、兩岸對照名詞-機械）與 ISO 7200 皆作 title block，無爭議。程式端 EXCLUDE_KINDS、TEXT_REGIONS 亦已用它。 |
|  | 比例尺 | drawing scale | `config.SCALE = 100 / PX_PER_CM` | 國教院：比例尺→scale（土木工程名詞）；作為量測工具的另有 draftsman's scale、proportional scale。本文件指圖面比例 1:100，故加 drawing 二字避開量尺義項。⚠ 不是 scale rule（三稜尺），也不是 scale bar（圖示比例尺）。s04_read 的 src_geom 即「端點距離 × 比例尺」。第一個可標位置… |
| △ | 民國 68 年 | 1979 (R.O.C. year 68) | `cases/68-01782` | 民國紀年 = R.O.C./Minguo era（民國年＋1911＝西元年）。專案裡「68 年案」是固定測資與第一筆迴歸測試，可標成 the 1979 case。 |
| ⚠ | 洗石子 | exposed aggregate finish | `classes.py material_note 構造詞庫` | ★ 主詞由 washed granolithic finish 改為 exposed aggregate (washed) finish：後者國際讀者較能理解，前者是港式規範用語。台灣定譯本次無法查證（樂詞網抓不到、WebSearch 額度用罄）。⚠ 若在文件中只在詞庫列舉裡出現一次，也可以選擇不標。 |
|  | 牆心線 | wall centerline | `axis_wkt（WALLS）／centerline・wall_axis（classes.py EXCLUDED）` | 國教院：中心線→centerline／CL（工程圖學、測量學）。程式端是 axis、圖學端是 centerline，同一件事，建議首次標「牆心線（wall centerline；plan 內為 axis_wkt）」。⚠ execution/exporters/ifc.py 逐字說明 axis_wkt 是牆心線，故必須用 add_wall_representation(of… |
|  | 疊圖 | overlay | `s03_lines.py 第 11 行閘門` | 國教院土木工程名詞正式收錄（疊圖分析 overlay analysis、疊圖描繪 overlay tracing）。把產出的向量圖疊回掃描底圖目視比對即 overlay／visual overlay check。★ 確認不要用 superimpose（偏學術）或 registration（那是配準演算法，指的是另一件事）。出處：§11 第 3 步閘門「疊圖目視 —— 牆心… |
| △ | 目測座標 | eyeballed coordinates | `COORD_SOURCES = (chain, measure) / tools.py measure` | §6.2 規則 3 與戒律一的禁令（「這是除了尺寸鏈以外唯一合法的座標來源——座標不准目測」）。英文口語 eyeballed 正是「用眼睛估的」，正式文件可寫 visually estimated coordinates。★ 國教院的「目測」對到 visual observation／ocular estimation／visual inspection，但那是「觀測」不… |
|  | 立面圖 | elevation view |  | ★ 主詞由 elevation 改為 elevation view，理由是同一份文件裡「標高」已佔用 elevation（見該條）。國教院有據：elevation view→立面圖（土木工程名詞-結構及材料）、front/rear elevation→前／後立面圖（工程圖學）。放棄的譯法：單用 elevation、facade drawing。出處：config.py 註… |
| △ | 粉刷 | cement plastering | `classes.py material_note 構造詞庫` | ★ 這是最容易誤譯的一個：台灣營建業的「粉刷」指水泥砂漿粉刷面層，不是油漆（painting）。英式 cement render／rendering，美式 cement plaster。相關：粗胚打底 ≈ scratch coat／base coat。 |
|  | 級高 | riser height | `stair` | 樓梯每階的垂直高度。riser 本指豎板，級高精確講是 riser height／rise。classes.py stair 立論「踏步數×級高＝層高，可交叉驗證 ASSUMED 層高」——★ 與「層高 floor-to-floor height」不可混。 |
| ⚠ | 街屋 | shophouse |  | shophouse 是東南亞／台灣建築研究常見譯法（與騎樓 qilou 同一批文獻），但也有人用 street house／row house／terraced house。本次未能查證定譯。出處：classes.py EXCLUDED 軸線條目「民國 68 年小型街屋／透天」。 |
|  | 衛浴廚具 | fixture | `fixture（classes.py）` | 限定為可窮舉的固定衛生設備與廚具（馬桶／浴缸／洗面盆／流理台），與 furniture（活動家具）刻意分開，classes.py 明寫「它不是 catch-all」。英文 plumbing fixtures／fixed equipment。它是「封閉小矩形→column」最大的假陽性來源。 |
|  | 製圖慣例 | drafting convention |  | 指圖面上約定俗成的畫法：線型、符號、標註方式、圖例。英文慣用複數 drafting conventions。⚠ 連帶把「製圖」定住：drafting（繪製行為與職能）／drawing（圖說本身）——絕不可譯 mapping 或 plotting。相關規範 CNS 3／ISO 128 系列。 |
|  | 詳圖 | detail drawing |  | classes.py hatch 判準提到的「大比例詳圖」= large-scale detail。 |
| △ | 詳圖索引 | detail callout | `detail_ref（classes.py EXCLUDED）` | 亦作 detail reference。放棄以 detail reference 為主詞：英美圖面實務口語一律說 callout。 |
|  | 踏面 | tread | `stair` | 樓梯的水平踏板面。classes.py stair 判準「踏面 26–30cm≈31–35px」。 |
|  | 軸線（軸網） | grid line | `grid_line / grid_axis（classes.py EXCLUDED）` | classes.py 裁定民國 68 年小型街屋多半沒有軸網，故不列入 15 類。軸號圓圈 = grid bubble（亦稱 grid mark）；整組是 column grid。 |
| ⚠ | 透天厝 | townhouse |  | townhouse 是常見英譯，但美式 townhouse 的意涵（連棟、共牆、常有社區管理）與台灣透天厝不完全對應；亦有人用 walk-up house／detached low-rise house。未查證。與「街屋」同一出處，兩詞最好一起定案。 |
| △ | 量體 | building mass |  | 指建築物的整體體量。classes.py level_line 判準「橫貫整個量體寬度」＝ the full width of the building mass。⚠ 不是 volume（體積）；設計行為叫 massing。 |
|  | 針筆 | technical pen |  | 即 Rotring／Rapidograph 那類製圖用針筆。classes.py wall 判準「1979 針筆 0.4–0.6mm≈5–7px」——這個線寬是規則式偵測器分辨牆線的物理依據。 |
|  | 門窗表 | door and window schedule | `schedule（TEXT_REGIONS／EXCLUDE_KINDS）` | element_tag 的 W1／D2 就是拿去對這張表的 join key，是「第三源」的來源之一。⚠ 英美實務常拆成 door schedule 與 window schedule 兩張表。⚠ 同一份文件裡 schedule 另有「時程」義（見「繪圖計畫」條的陷阱），三者不可混。 |
| △ | 閉合 | closure | `chain_closure / chains.closed` | 測量學用 closure；尺寸鏈加總對不上即 closure error／misclosure。國教院無「尺寸鏈閉合」複合詞條，故標常見用法。程式端已釘死 closure（CONFLICT_SOURCES 的 chain_closure、CHAINS 的 closed 欄，另有 sum_value／total_value／delta）。第一個可標位置：§7.1 第 306 行。 |
|  | 開口 | opening | `opening（classes.py）` | 門窗未定案時的低信心父類。IFC 對應 IfcOpeningElement。 |
| ⚠ | 雨遮 | canopy |  | ★ 主詞由 rain canopy 改為 canopy：rain canopy 在建築英文不是慣用搭配。台灣不動產與法規英譯不一致（eaves／canopy／rain shelter／hood 都見過），本次法規英譯站抓不到、WebSearch 額度用罄，無法查證。★ 請負責人定一個再寫進對照表。出處：classes.py EXCLUDED 凸包條目。 |
| △ | 騎樓 | arcade | `classes.py room_label 室名詞庫` | 英文維基 Five-foot way 條目直接以 arcade 對應 qilou，並列 five-foot way／verandah，台語 têng-á-kha。★ 台灣建築技術規則官方英譯本次仍無法核對（法規網是 JS 頁面，抓回空白），故不列為標準用語。另見 covered walkway／covered arcade。出處另有 classes.py EXCLUDED… |

## BIM/CAD（12）

BIM 與 CAD 領域用語。

| | 中文 | 英文 | 對應識別碼 | 備註 |
|---|---|---|---|---|
| ★ | 匯出器 | exporter | `execution/exporters/ / class Exporter / get_exporter()` | 中文「匯出器」與英文 exporter 一對一，無爭議；該 ADR 內文已混用中英。目前六條路：dxf（MVP）／ifc／archicad／rhino／revit／visualarq。 |
| △ | 原生物件 | native object | `exporters/base.py 的 native_objects / registry.table() 的 native 欄` | 指匯進去是真的牆／柱物件（native element／native wall），而不是一堆線或 DirectShape。§8.1 釘死的事實之二：Autodesk/revit-ifc 的 importer 只建 DirectShape／DirectShapeType／Level／Grid。BIM 互通文獻慣用 native object／native element，非… |
|  | 參數化物件 | parametric object | `exporters/base.py 的 native_objects 屬性` | 指寫出可被 BIM 軟體識別為可編輯物件的參數化幾何，而非烘焙成三角網格。單用「參數化」時作 parametric（§8.1「IFC exporter（IfcOpenShell，參數化寫法）」），不另立條。 |
| △ | 垂直對位 | vertical alignment | `工具 compare_floors` | classes.py column／stair 立論（跨樓層垂直對位與樓板開孔的錨點）。compare_floors 給中樞同位置多樓層並排影像，供它判柱位是否對齊。 |
|  | 實體 | entity | `dxf.py 的「AEC 實體」` | ⚠ 同一個中文詞在本專案有兩個意思，標註時必須分開：(1) dxf.py 的「AEC 實體」= DXF entity（本條）；(2) rhino.py 的「封閉實體」= closed solid（另立一條）。★ 不要用同一個英文帶過。IFC 側的「實體欄位」官方叫 attributes（GlobalId 即 IfcRoot 的 attribute）。 |
|  | 封閉實體 | closed solid | `execution/exporters/rhino.py` | rhino.py 列為相對於 DXF 的加值：可算體積、可布林。對應 rhino3dm 的 Extrusion（IsSolid=True）。見上一條的兩義警告。 |
|  | 布林運算 | Boolean operation |  | CAD 通用詞。rhino.py「可算體積、可布林」；ifc.py 說我們的資料「沒有曲面、沒有複雜 boolean」。 |
| ⚠ | 幾何與屬性等價 | geometry-and-attribute equivalence | `execution/exporters/base.py / ADR 0007` | 本專案自創片語、非業界術語。ADR 0007 對確定性的重定義：排除 GUID 與時間戳後比對，且 id 盡量從 judgment id 決定性導出。用字上 attribute 選得對——IFC 的實體欄位官方就叫 attributes，故此譯在 BIM 語境讀得通。⚠ 不建議替換成 semantic equivalence（涵蓋範圍不同）。對照組 byte-identi… |
|  | 往返測試 | round-trip test | `§12 的「ArchiCAD 四道牆往返測試」` | 資料交換領域標準詞：匯出再匯回、看資訊掉了多少。亦寫 round-tripping；四道牆往返測試 = four-wall round-trip test。⚠ ifc.py 自己指出這個閘門有邏輯問題（見「閘門」條）。 |
|  | 熱連結 | Hotlink | `execution/exporters/ifc.py 第 38 行` | ★ 信心已上調：Hotlink 是 Graphisoft ArchiCAD 的正式功能名（Hotlink Module），繁中介面即作「熱連結」，不是自譯，故首字大寫。出處逐字：「匯入 ArchiCAD 要用 Open 或 Merge、**不要用 Hotlink**（熱連結不可就地編輯）」。 |
|  | 牆型別 | wall type |  | Revit 的 wall type、ArchiCAD 的牆工具設定；指帶構造層次與屬性的牆類型定義，正是純幾何核心（Rhino）沒有的東西（ADR 0007「Rhino 沒有牆型別」）。 |
| △ | 粗胚 BIM | draft BIM | `config.DRAFT_SUFFIX = "_draft" / ASSUMED_MAX_CONF = 0.4 / ASSUMED_SRC = "assumed"` | ★ 四組結論不一，取與程式端一致的 draft BIM（BIM 界確實沒有對應標準詞）。CLAUDE.md 寫明「粗胚 BIM 被誤當精確模型是 v0.3 風險表上的中度風險」。放棄的譯法：bare-shell BIM、rough-shell BIM、coarse BIM、preliminary BIM。⚠ 重要：「粗胚」在營建原義是未粉飾的結構體（粗胚屋 ≈ bare … |

## 影像處理（31）

影像處理與電腦視覺用語。

| | 中文 | 英文 | 對應識別碼 | 備註 |
|---|---|---|---|---|
| ★ | 偵測類別 | detection class | `core/classes.py 的 15 類＋追加 / class_name 欄` | 物件偵測領域標準說法 class／detection class；classes.py 內部欄位名就是 class_name。★ 順序即 id、永不重排（ADR 0003），新類別只能 append 到 id 15 起（`calc_note` 是第一個追加，待負責人核可）。單說「類別」時就是 class。 |
| △★ | 拆片 | tiling | `tile / tile_id / 01_tiles/ / s01_ingest` | 程式端已釘死 tile／tiles（OFFSETS.tile_id 是第一欄），故名詞用 tile、動作用 tiling 與識別碼一致。沒有公認的動詞形式，split into tiles 也通。s01_ingest 首行即「PDF 拆片（免拼接）」。第一個可標位置：v0.4 變更 3（第 40 行）。 |
| ★ | 線段 | line segment | `03_lines.csv / fields.LINES` | 機率式霍夫轉換（cv2.HoughLinesP）的輸出即 line segments。CLAUDE.md「649 條原始線段」= 649 raw line segments。 |
| ★ | 自適應二值化 | adaptive thresholding | `cv2.adaptiveThreshold / config.ADAPTIVE_BLOCK, ADAPTIVE_C = 31, 12` | ★ CLAUDE.md §語言慣例已把「自適應二值化（adaptive thresholding）」釘死為示範用例（第 144 行），照用。★ 放棄 adaptive binarization——OpenCV 的函式就叫 adaptiveThreshold，讀者照 binarization 查文件會找不到；binarization 可放 note。二值圖 = binary… |
| ★ | 規則式偵測器 | rule-based detector | `perception/detectors/rule.py / DETECTOR_ID = "rule_v1" / 03_detections 的 detector 欄` | 與學習式（YOLO）相對，rule-based 是機器學習領域的標準對立詞。detectors/yolo.py 目前是空殼（§12 明令等存量配對盤點的數字）。 |
| △★ | 轉正 | rotate upright | `01_tiles_upright/ / OFFSETS.upright_file, rotation / s01b_orient` | ★ 主詞取 rotate upright（與識別碼 upright 一致），放棄 orientation correction 當主詞（會與另一條詞「方向偵測 orientation detection」撞）。★ 三組一致的警告：絕不可譯 deskew——deskew 專指幾度的小角度歪斜校正，s01b 處理的是 0/90/180/270 的整體正交方向（68 年案第 1… |
| △ | 候選框 | candidate box | `03_detections 的 bbox_wkt` | detectors/rule.py「三條規則（從線段反推候選框）」。bbox = bounding box。 |
|  | 假陽性 | false positive |  | classes.py fixture 立論：衛浴廚具是「封閉小矩形→column」最大的假陽性來源。 |
|  | 凸包 | convex hull | `outline / elevation_outline（classes.py EXCLUDED）` | classes.py EXCLUDED 條目已自帶英文對應。 |
|  | 局部截圖 | crop | `04_crops/ / 工具 crop_look` | 讀數用的小圖，中樞可用 crop_look 親眼看。⚠ 不要用 screenshot（那是螢幕擷取）。 |
| △ | 平行線對 | parallel line pair | `EVIDENCE_NS 的 pair# → (detections, det_id) / detectors/rule.py 第一條規則` | 兩條夾角<3°、法向間距落在 config.WALL_GAP_PX (12–42px) 內的線＝一道牆的候選。★ 建議一定要加 parallel：光學／解析度領域的 line pair（lp/mm）是完全不同的意思。非業界固定詞。 |
|  | 拼接 | image stitching |  | 電腦視覺標準詞（全景接圖）。本專案刻意不做拼接，改用 PDF 內的放置矩陣——這個詞的準確性關係到讀者理解 s01 的設計理由。延伸：s01_ingest 明令「不要做特徵點拼接」= feature-based image stitching（特徵點 = keypoints／features）。 |
| △ | 摺痕 | crease | `QUALITY_LEVELS 的 crease / planning/skills/crease_repair` | A1 曬圖對摺留下的折線。國教院查無製圖領域詞條（只有畜牧、紡織、材料、視覺藝術的 crease／fold／score／crimp），故標常見用法；程式端已釘死 crease，直接沿用。紙圖對摺的痕跡英文也說 fold mark，但 ⚠ fold line 在製圖裡另有意思（摺線），避免。⚠ 規格第 280 行在 §6.1 plan 逐字 yaml 區塊內不可加註；第一個… |
| △ | 擴散 | ink bleed | `QUALITY_LEVELS 的 bleed` | 亦說「暈開」。classes.py wall 判準「曬圖擴散後常糊成單一粗帶」——這是 12cm 內牆的平行線對會糊掉的原因。程式端字根 bleed。 |
| △ | 放置矩陣 | transformation matrix | `01_offsets.csv / s01_ingest.py` | ★ 主從已對調（原兩組提案主詞為 placement matrix）。已在本機驗證 PyMuPDF 1.27.2：Page.get_image_rects(name, transform=False) docstring 原文寫「whether to also return the transformation matrix」，回傳 (Rect, Matrix)；page… |
| △ | 整頁座標系 | whole-page coordinate system | `01_offsets.csv 的 x/y` | 指以整張圖紙為基準（而非單片 tile）的座標；tile 座標→頁座標的換算就是 01_offsets 的 x/y。片內座標 = tile-local coordinates。非業界固定詞，是本專案對一般概念的中文說法。這也是 §3.1 警告「轉正要同步重算 x/y」的原因。 |
|  | 方向偵測 | orientation detection | `s01b_orient.py 的 ROTATIONS = (0, 90, 180, 270)` | OCR 領域的標準說法（page orientation detection）。本專案用文字方向投票決定。 |
| △ | 版面 | sheet layout | `02_sheets.csv / s02_layout` | 指一張圖紙上「這是什麼圖、哪一層、什麼比例、圖框在哪」。⚠ 不是排版 typesetting，⚠ 也不是 CAD 的 Layout 分頁（那是另一件事，最容易誤讀）。程式端模組名已是 s02_layout，故 layout 這半邊釘死；加 sheet 二字是為了與上一條「版面判讀」分開。第一個可標位置：§3 第 140 行。 |
|  | 版面判讀 | document layout analysis | `perception/s02_layout.py` | ★ 信心由常見用法上調為標準用語：document layout analysis 是文件影像分析領域的定名。s02 是用 VLM 做，但術語不變：一次讀出圖種／樓層／比例／單位／方向／圖框／標題欄／版本線索。 |
| △ | 筆畫密度 | stroke density | `fields.QUALITY 的 stroke_density 欄` | 國教院查無詞條。指 256px 區塊內線條墨色佔比；stroke density 在 OCR／手寫辨識文獻常見，ink density 也通。★ 順手回報一個不一致：core/case.py 對 02_quality.csv 的註解寫「對比度/雜訊/可信度」，但 fields.QUALITY 的第三欄實際是 stroke_density（筆畫密度）不是可信度，兩處措辭需對齊。 |
| △ | 覆蓋率 | coverage | `fields.RESIDUALS 的 coverage 欄 / orchestrator.COVERAGE_OK` | 國教院的「覆蓋率」詞條（area coverage rate、surface coverage、fault coverage ratio）都在別的領域。此處指 judgment 幾何投影回像素後蓋住真線的比例，0–1，程式端已用 coverage。⚠ 門檻 COVERAGE_OK 目前沒有實測依據（§4.1 自陳）。 |
| △ | 詞庫 | lexicon | `classes.py 的室名詞庫、構造詞庫` | 亦可寫 keyword list／vocabulary；lexicon 較貼近「一份對照用的詞表」。classes.py 用「正面詞庫定義」對比「補集定義」——後者會在每次 append 新 id 時無聲改變語意，是排除 text_note 的理由。 |
|  | 質心 | centroid |  | classes.py room_label 判準「質心落在 wall 線段圍出的封閉區內」。 |
|  | 輪廓 | contour |  | OpenCV findContours。與連通元件同一句出處（classes.py column 的 detect 說明）。 |
|  | 連通元件 | connected component |  | OpenCV connectedComponents；完整流程稱 connected-component labeling。classes.py column 判準指出柱邊長低於 MIN_LINE_LENGTH，必須改用連通元件／輪廓抓實心塊，不能靠線段。 |
|  | 連通性 | connectivity | `工具 trace(line_id)` | 影像處理的 connectivity（4/8 連通）與圖論的連通同字。此處是沿斷線追蹤是否可接上（摺痕斷線用），用 connectivity 沒問題。 |
|  | 霍夫轉換 | Hough transform | `config.HOUGH_THRESHOLD, MIN_LINE_LENGTH, MAX_LINE_GAP = 60, 60, 6` | ★ 四組都沒單獨抽出，但 config.py 有三個對應參數、s03_lines 全靠它，且是本任務判準 B 的舉例詞，故補收。本專案用的是機率式霍夫轉換 = probabilistic Hough transform（cv2.HoughLinesP），標註時建議寫全。 |
|  | 配準 | registration | `perception/s01c_register.py` | 影像處理裡以像素內容求變換矩陣的演算法族（相位相關、特徵匹配、ECC）。⚠ **與「疊圖 overlay」不是同一件事**（見該詞 note）。ADR 0009 實測五種配準法在曬圖平面圖上全數失敗，故本專案**不用像素配準**求片間關係；模組名 s01c_register 是 ADR 0008 留下的舊名，ADR 0011 換掉機制但保留檔名。 |
|  | 接縫 | seam | `01_placement.csv 的 seam_* 欄` | 兩片相鄰影像交界處。影像拼接領域標準用語（seam finding／seam carving）。本專案指同一張大圖被影印機切成數張 A3 後，兩張 A3 相接的那一帶。 |
| △ | 比例尺校準 | scale calibration | `01_placement.csv 的 px_per_cm 欄 / config.PX_PER_CM` | 由圖上已知的真實長度反推每公分佔幾像素。★ config.PX_PER_CM = 1.1811 是 68 年案 DPI 300、1:100 的實測值；79 年案實測 1.207（+2.2%），差距不大但仍建議每案校 —— 806cm 總寬上是 18cm。ADR 0011 改為每片由閉合的尺寸鏈自行校出。⚠ 換算時要看清座標空間：判讀是在縮小的區塊圖上做的，比例座標要乘 meta.w（整片像素）而不是乘縮圖寬。 |
|  | 度量 | metric | | 與「拓樸 topology」相對：拓樸只講相鄰與順序（哪張在左上），度量講差幾個像素。ADR 0009 只做拓樸（「排列」），ADR 0011 補上度量（「對位」）。 |

## 軟體架構（48）

軟體架構與工程用語。

| | 中文 | 英文 | 對應識別碼 | 備註 |
|---|---|---|---|---|
|  | 實體解析 | entity resolution | `s09_crosscheck / plan 的 conflicts` | 判定不同來源的多筆記錄講的是不是同一個東西。ADR 0009 已指出「跨片的實體解析要成為中樞的判斷」；p03 實測四個區塊把同一條屋頂邊界取了四個不同名字，就是這個問題。資料整合領域標準用語，別譯 entity matching（那偏指配對步驟本身）。 |
| ★ | 契約 | contract | `core/fields.py / core/classes.py / core/plan_schema.py` | design by contract 的 contract，指介面與資料格式的約定。⚠ 不是法律合約（那是 agreement）。本專案指這三個「只能引用、不能改」的檔。 |
| ★ | 架構決策紀錄 | Architecture Decision Record, ADR | `docs/adr/` | ★ CLAUDE.md §語言慣例已把「架構決策紀錄（Architecture Decision Record, ADR）」釘死為示範用例（第 142 行），格式含縮寫，照抄。業界標準詞（Michael Nygard 2011 提出）。狀態機 proposed／accepted／superseded ＋ superseded_by 也是 ADR 慣例欄位。⚠ 模板段名「脈… |
| ★ | 殘差 | residual | `plans/residuals_vN.csv / fields.RESIDUALS / RESIDUAL_KINDS / residuals_all_handled()` | 統計與量測領域標準詞。此處指 s08_compare 把 judgments 幾何投影回像素後，plan 畫出來與掃描底圖對不起來的部分。三種 kind 已是英文（unexplained_stroke／low_coverage／over_reach）不必另標。 |
| ★ | 流程控制器 | orchestrator | `planning/orchestrator.py` | 程式端識別碼即權威：檔頭 docstring 自稱「全系統唯一的流程控制器」，並明寫「for 迴圈、終止條件、退件，全在這支程式碼；中樞永遠只被呼叫」。直接沿用 orchestrator。 |
| △★ | 矛盾 | conflict | `08_conflicts.csv / fields.CONFLICTS / CONFLICT_SOURCES = (chain_closure, plan_vN, cross_storey)` | ⚠ 文件講「矛盾」，程式碼一律是 conflict。★ 建議統一標 conflict 以對齊識別碼，不要譯 contradiction 造成兩套詞。CLAUDE.md「矛盾是產出，不是障礙」「帶著矛盾收工」都是這個欄位語意；三來源以 append 模式共寫；review/export_review.py 的去重 = conflict de-duplication。 |
| ★ | 確定性 | determinism |  | 形容詞用 deterministic（確定性管線 = deterministic pipeline、確定性執行 = deterministic execution），名詞 determinism。與 LLM 的非確定性相對，是全系統的核心分界。★ 重要：execution/exporters/base.py 與 ADR 0007 把它重新定義過——不是 byte-iden… |
| ★ | 脈絡 | context | `planning/context.py / plan_schema.CONTEXT_KEYS = (kind, floor, scale, unit, orientation)` | ⚠ 一字兩義但同一個英文，標註時看上下文即可：(1) LLM 的脈絡窗（context window）——§7.1 脈絡節食、撐爆脈絡、按需載入、plan 的 context 區塊；(2) ADR 模板的固定段名「## 脈絡」= Context 段（模板該段寫「寫事實，不要寫結論」）。⚠ 中文「脈絡」易被誤讀成血管／紋理，值得標一次。台灣慣用「脈絡」、大陸慣用「上下文」，… |
| △★ | 退件 | rejection | `propose_plan(..., rejected=problems) / plan_schema.REJECT_RULES / OVERRIDE_ACTIONS` | 借自建照送審／公文的「退件」，指 validate 不過就把 plan 退回中樞重寫（同輪一次為限，控制權在 orchestrator）。名詞 rejection、動詞 reject——程式參數名就是 rejected，標註須與識別碼一致。★ 不要用 return（會被讀成回傳值）；要強調「打回去重寫而非否決」時可補 returned for revision，但不當主詞… |
| △★ | 閘門 | gate | `exporters 的 gated / gate_reason（registry.py 丟 PermissionError）` | 指「這關沒過就不准往下做」的判準。專案管理說 gate／stage gate，CI 說 quality gate，工程說 gating criterion；當動詞用時 gated on（IFC 被 §12 gated on 四道牆往返測試）。§8.1 末已指出 §12 的 IFC 閘門本身有邏輯問題（閘門和被擋的東西是同一件事）。第一個可標位置：v0.4 變更 7（第 4… |
| △★ | 驗收 | validation | `planning/validate.py 的 validate() / plan_schema.REJECT_RULES（正好六條，對應 §6.2）` | ★ 陷阱確認成立且重要：在營建與採購語境「驗收」是 acceptance inspection（《政府採購法》英譯第五章章名即 Inspection and Acceptance），但本文件指的是程式對 plan 做 schema／證據檢查。**必須標 validation，絕不可標 acceptance** —— 在一份談建築的文件裡這個誤讀成本很高。⚠ 但「驗收條件」… |
|  | 供應商 | provider | `planning/llm/base.py 的 class Provider / registry.py 的 PROVIDERS` | LLM 業界一律用 provider（不是 vendor／supplier）。⚠ 中文也說「廠商」，但那指 Anthropic／OpenAI 這些公司（vendor）；介面層一律 provider。「供應商介面」= provider interface，由本條與「薄介面」組成，不另收。 |
|  | 個資 | personal data |  | ★ 已查證法源：全國法規資料庫英文版確認《個人資料保護法》官方英譯為 Personal Data Protection Act，故 personal data 是法源用詞。美式語境常說 PII（personally identifiable information），可附。此處是本地部署（金鑰走環境變數、不進 core/config.py）的理由，語意重要。可標位置：第 … |
|  | 前置資料 | front matter | `planning/skills/__init__.py 的 _front_matter()` | 完整說法 YAML front matter（Jekyll／Hugo 等靜態網站生成器的既定用語）。兩處同物：SKILL.md 檔頭的 name／description，以及 ADR 檔頭兩條 --- 之間的 number／title／status／date／supersedes／superseded_by 六個鍵。「前置資料」不是通用中譯、是本專案（與 smoke.py… |
|  | 副作用 | side effect |  | 函數式程式設計的標準詞。§13 派工格式「不要修改 plan，不要有副作用」= no side effects（模組不得順手回寫檔案）。 |
|  | 命名空間 | namespace | `core/fields.py 的 EVIDENCE_NS` | evidence 一律寫成「命名空間#id」：pair#12、chain#c003、read#p02_d0038、det#…。程式常數已用 NS 縮寫。 |
| △ | 回寫 | write back | `orchestrator.apply_plan_status()` | 把 plan 的裁定寫回 03_detections／03_elements 的 status 欄。⚠ 必須在 validate 之後、execute 之前（腦手分離）。 |
|  | 失效模式 | failure mode |  | 工程可靠度領域標準詞（FMEA 的 failure mode），用在此處指「壞掉的時候會怎麼壞」。出處：s04_read.py／§1.2「VLM 的失效模式」。 |
| △ | 定版 | final | `fields.py／classes.py 檔頭的【定版】／【草案】標記` | ★ 英文由 finalized (locked) 改為 final：【定版】是與【草案】(draft) 成對的檔頭標記，英文成對慣例就是 draft／final，用 finalized 會拆散這組對仗。動詞化語境（「已定版」）才用 finalized；要強調不可改可寫 final (locked)。★ 順手回報一個不一致：CLAUDE.md 說三件套「由負責人親手寫」，但… |
|  | 容差 | tolerance | `s04_read.py 投票規則` | 「三源一致（差異在容差內）→ green」。⚠ 此處是數值比較容差，不是製造公差（公差英文也是 tolerance，同一份文件裡語境要分清），也與 §6.1 的 confidence（信心）不同層次。營建與工程雙邊都用 tolerance。 |
| △ | 實測校準參數 | measured calibration parameters | `core/config.py（規格 §10）` | 指 PX_PER_CM、ADAPTIVE_BLOCK/C、HOUGH_THRESHOLD、MIN_LINE_LENGTH、MAX_LINE_GAP 等。「實測」= 在 68 年案上真的量過而非理論推導，可寫 empirically measured／field-measured。★ 與同檔的 ASSUMED（假設值 assumed values，ASSUMED_SRC="… |
| △ | 工具規格 | tool spec | `planning/tools.py 的 TOOL_SPECS（8 支）/ TOOL_NAMES` | ★ 已在原始碼實地清點：TOOL_SPECS 共 **8** 支不是 6 支——crop_look／redetect／read_number／measure／trace／compare_floors／ask_arbiter／ask_detective，TOOL_NAMES 由 TOOL_SPECS 導出（smoke.py [7] 會查兩者對得上）。指這批工具的 JSON … |
|  | 幻覺 | hallucination |  | LLM 領域已定型的標準詞。§1.2 的核心論證：傳統 OCR 錯成 36OO 一眼看得出，VLM 會給你乾淨、合理、有自信、而且錯的 3600——對矛盾偵測而言幻覺遠比誤認致命。這是三源設計的理由，務必標。 |
|  | 技能 | skill | `planning/skills/<name>/SKILL.md（＋選用 run.py）` | 一個資料夾一個技能，SKILL.md 必要。與 Anthropic Agent Skills 同名同構，故標 skill／Agent Skill 而非 capability。載入器提供 list_skills()／load_skill()／run_skill()；目前只有 crease_repair 一個。 |
| △ | 投機性抽象 | speculative generality |  | ★ 主副詞已對調：CLAUDE.md 工作準則「移除投機性抽象、預先設計的擴充點」講的正是 Martin Fowler《Refactoring》既有的壞味道 **Speculative Generality**，標它才有出處；speculative abstraction 是逐字直譯、沒有文獻來源。若要忠實於中文字面，可寫「投機性抽象（speculative genera… |
|  | 拓樸 | topology |  | 「尺寸是幾何，線段只是拓樸」（s05_solve.py、classes.py）。台灣寫「拓樸」、大陸寫「拓扑」，英文同為 topology。 |
|  | 按需載入 | on-demand loading | `planning/skills/__init__.py 的 load_skill(name)` | ★ 信心已上調：按需載入 ↔ on-demand loading 是電腦領域長期固定的對譯，不是本專案自組。脈絡裡平常只放技能名與一行描述，碰到對應情境才載入全文。工程語境也常說 lazy loading。⚠ Anthropic 描述同一機制時用 progressive disclosure，但那是別人的用詞，不建議替換。 |
| △ | 校對 | review | `review/ 套件 / review/export_review.py / 06_verified.csv` | ★ 兩組衝突，取與程式端一致的 review。指人拿 Excel 核圖：s09 跨樓層檢查 → Excel 校對 → 人。⚠ 絕不可譯 proofreading（那是文字校對，這裡是人確認 AI 讀值）。營建製圖語境的人工核圖英文是 drawing check／checking（做這件事的人是 checker），必要時可寫成「校對（review；製圖語境的人工核圖亦稱 d… |
| ⚠ | 派工單 | work order | `規格 §13` | 「派工格式」與「派工單」同義，合併為一條。§13：引契約、指段落、定 I/O、禁副作用、附測資。英文沒有既定詞：work order 是營造／設備維修工地的標準對譯，借喻在這個做營建題目的專案裡反而讀得通；軟體語境更貼的是 task spec／task brief／assignment spec。⚠ 不要用 work package（那是 WBS 的專名，層級意思不同）。… |
| △ | 測資 | test data | `tests/fixtures/make_case_min.py` | 台灣程式圈慣用「測資」（大陸多說「測試資料」）。★ 複合詞要分別譯、不要一律套 test data：具體那份最小案件資料 = (minimal test) fixture；「通過性測資」= positive test case。⚠ validate.py 警告不可拿 examples/plan_vN.sample.yaml 當通過性自測（那裡的 id 是假的）。 |
|  | 煙霧測試 | smoke test | `tests/smoke.py` | 32 支模組 import ＋ §10 參數比對 ＋ §11 進度。⚠ 其中 [9] 就是擋「逐字轉錄區塊被加註」的那一項——本術語表的標註工作會被它檢查。 |
|  | 硬相依 | hard dependency | `exporters/base.py 的 needs_addon` | 指少了它就跑不起來的相依，與 optional dependency 相對。archicad.py 逐字寫 needs_addon =「Tapir Add-On ≥ 1.5.7（MIT，第三方，硬相依）」；§8.1 三件釘死的事實第 3 條「MCP 不得成為匯出器相依」。 |
|  | 空殼 | stub | `perception/detectors/yolo.py` | 只有介面沒有實作的佔位模組（亦作 placeholder）。CLAUDE.md 已直接用英文 stub 描述整個 repo 現況。⚠ 與「假中樞 fake proposer」不同：空殼不能跑，假中樞是會回固定值的可執行替身。 |
| △ | 範圍紀律 | scope discipline |  | 出自工作準則首句「KISS、YAGNI、最小差異（minimal diff）與範圍紀律」。⚠ 該句的 minimal diff 原文已在 CLAUDE.md 標好，KISS／YAGNI 本身是英文縮寫不需再標。scope discipline／scope control 都是實際在用的英文（後者偏 PM 流程），與 §12 禁止清單、「驗收條件通過後即停止擴張範圍」同向。 |
|  | 終止條件 | termination condition | `residuals_all_handled()` | 迴圈的停止判準：「終止＝殘差皆已修或已報告」。也寫 stopping condition／exit condition，但 termination 最貼「終止」。 |
| △ | 統一層 | unified LLM API layer |  | ADR 0005 選項表「LiteLLM 之類統一層」。LiteLLM 自稱 unified interface／LLM gateway，故此譯有出處。⚠ 譯 abstraction layer 也懂，但「統一」那層意思會掉，不建議。 |
| △ | 自測 | self-test | `規格 §13「每支模組自帶自測」` | 指模組內附的檢查程式。英文更常說 module-level test／built-in self-check；self-test 可懂但不是強標準詞。⚠ built-in self-test (BIST) 在硬體領域另有專指，別誤用。本次未能查證業界慣用頻率，維持常見用法。 |
| △ | 萬用桶 | catch-all | `classes.py EXCLUDED 的 symbol / unknown / other` | 英文其實已在原文裡：ADR 0003 後果段下一句就寫「catch-all 是 classes.py 的合法成員」，classes.py 的 fixture 判準也自己寫「它不是 catch-all」。⚠ 所以第一次標「萬用桶（catch-all）」後，緊接著那句的 catch-all 不必再處理。bucket 只是讓詞性更像名詞，可省。 |
| △ | 薄介面 | thin interface | `planning/llm/base.py` | ADR 0005「LLM 供應商用自寫薄介面，不用統一層套件」：Provider 只包住真實需求那幾個參數（單輪、同步、帶工具、帶圖）。thin wrapper／thin adapter layer 亦常見，但此處是介面定義而非包裝既有物件，thin interface 最貼。 |
| △ | 裁定 | adjudication | `orchestrator.apply_plan_status() / fields.STATUS = (proposed, adopted, rejected, uncertain)` | ★ 與上一條刻意分家。本條指**中樞**在 plan 裡把 status 從 proposed 改成 adopted／rejected／uncertain（動詞 adjudicate a label）。與 sub-agent 的「只給建議、不做定案」相對。⚠ 絕不可與人的「裁決 ruling」共用同一英文。 |
| △ | 裁決 | ruling | `docs/待決事項_裁決.md` | ★ 一詞兩義已拆成兩條。本條指**人**的裁決：待決事項.md →（負責人裁決）→ 待決事項_裁決.md → 折進規格與 ADR；fields.py 內大量「裁決 §1／§5／§6」引註；CLAUDE.md「本機的裁決文件優先於規格書」。動詞 rule on。⚠ 不用 decision（已被 ADR 的「## 決定」段與 Architecture Decision Rec… |
| △ | 負責人 | owner |  | 指手寫／核定契約三件套、做裁決、被「停下來問」的那個人（CLAUDE.md §0.4、fields.py 檔頭「需負責人逐項確認」、classes.py「此格需負責人知情裁決」）。⚠ 切勿套用公司法語境的「公司負責人／代表人」（《公司法》英譯作 responsible person，亦見 principal），那是完全不同的意思；需消歧義時寫 project owner。… |
|  | 轉接器 | adapter | `planning/llm/anthropic.py, openai_compat.py` | GoF 的 Adapter pattern，把各廠商 SDK 轉成 base.Provider 介面。台灣也譯配接器／適配器，但本專案文件已一致使用「轉接器」。openai_compat 一支涵蓋 OpenAI／Ollama／vLLM／LM Studio。 |
|  | 迴歸測試 | regression test |  | 軟體測試標準詞，無爭議。§11 第 4 步「68 年案 403+403=806 當第一筆迴歸測試」；s01b_orient 的「第 1 頁是 180°」同屬。⚠ 同一份 CLAUDE.md 另有「回歸風險」（regression risk），繁中「迴歸／回歸」兩種寫法並存，英文都是 regression。 |
| △ | 邊界情境 | edge case |  | CLAUDE.md「測試聚焦於本次變更的核心行為、合理邊界情境與實際回歸風險」。⚠ 不宜用 boundary case／boundary value——那特指數值邊界（boundary value analysis 是測試方法學的專名），語意較窄。 |
|  | 風險表 | risk register |  | ★ 信心已上調：專案管理標準詞（PMBOK 用語）。CLAUDE.md 與 config.py 都寫「粗胚 BIM 被誤當精確模型是 v0.3 風險表上的中度風險」；中度風險 = medium severity／medium risk。⚠ risk table 是字面直譯，實務少見，放棄。 |
|  | 驗收條件 | acceptance criteria |  | CLAUDE.md 工作準則「滿足明確需求與驗收條件」——這是專案管理意義的驗收條件，與上一條 §6.2 的「驗收（validation）」語意不同，⚠ 不要用同一個英文。延伸：「驗收閘門」= acceptance gate（CI 領域更常見的既有詞是 quality gate）。 |
| △ | 骨架 | skeleton | `core.case.ensure_case()` | 三處語境要分開逐句選，不要統一：「規格書＋骨架」「orchestrator 的迴圈骨架」= (loop) skeleton，與 stub 搭配（repo 幾乎全檔是 raise NotImplementedError）；「目錄骨架」用 scaffold 較自然——ensure_case() 只建資料夾不碰 CSV，正是 scaffolds the folders。 |

## 本專案自創（26）

本專案自己的比喻詞，**沒有業界標準英文**。有對應識別碼的以識別碼為準。

| | 中文 | 英文 | 對應識別碼 | 備註 |
|---|---|---|---|---|
| △★ | 三源 | three sources | `fields.READING_SOURCES = (src_paddle, src_vlm, src_geom) / fields.py 分節標題「04 三源讀取」` | 本專案自創（規格 §1.2）。★ 完整說法 three independent sources（強調「機制上獨立」），行文中用 three sources 即可，兩者不衝突。延伸：三源讀取 three-source reading、三源投票 three-source voting（VOTE_STATUS = green／yellow／red）、第三源 the third … |
| △★ | 中樞 | planning | `planning/ / propose_plan() / proposer.py` | 本專案自創的比喻詞（字面取自中樞神經）。★★ 硬規則不是偏好：ADR 0001 與 v0.4.1 變更 8 明定「英文 brain 一詞自此退出 repo，概念一律用中文『中樞』」，已 grep 確認 .py 檔無 brain 識別碼（只殘留在三處敘述改名歷史的文字裡）。**絕對不可標 brain**，也不要標 central hub／central nervous sy… |
| △★ | 品質地圖 | quality map | `perception/s02b_quality.py → 02_quality.csv / fields.QUALITY / QUALITY_LEVELS` | 本專案自創、非業界標準詞：每 256px 區塊算對比度／雜訊／筆畫密度後分級（欄位 contrast／noise／stroke_density／level，等級 good／shadow／crease／bleed，純統計無 AI）。分級值已是英文不必標。context.py 說的「差區」＝ level != good 的區塊，可寫 poor-quality zones。⚠ … |
| △★ | 富標籤 | rich label | `core/fields.py 的 RICH_LABEL = (conf, evidence, provenance, quality_zone, status)` | 本專案自創，但直譯有程式碼背書：DETECTIONS 與 ELEMENTS 都以它結尾。語意是「標籤是主張不是事實（claims, not facts）」，帶證據與來源。生命週期 proposed →（中樞裁定）adopted／rejected／uncertain 見 fields.STATUS。⚠ 欄位名 quality_zone（品質區）依判準不另收。第一個可標位置：… |
| △★ | 手 | execution | `execution/（v0.4.1 變更 7 由 hands/ 改名）/ s07_execute.py` | 本專案自創的比喻詞。s07_execute.py 首行 docstring 即「s07_execute —— 手：照計畫確定性執行」。⚠ 不要譯 hands。⚠ 位置注意：單獨的「手」字在規格內只出現在第 122 行（§2 ASCII 圍欄內）與「腦手分離」一詞裡，圍欄內加括號會破版，建議改標在 §8 s07_execute 敘述或 CLAUDE.md 的套件說明處。 |
| △★ | 排除帶 | exclusion zone | `02_exclude.csv / fields.EXCLUDE / EXCLUDE_KINDS = (frame, title_block, stamp, schedule, other)` | 本專案自創，程式端只有短名 exclude。zone 或 band 都可（圖框那一圈是帶狀，band 更貼字面），但 exclusion zone 英文較常見、也不會與 fields.QUALITY 的 block 混淆，故取 zone。⚠ 四個 kind 本身是製圖標準詞，值得在同一段順帶標好：圖框 drawing frame、標題欄 title block、門窗表 d… |
| △★ | 繪圖計畫 | drawing plan | `plans/plan_vN.yaml / propose_plan() / core/plan_schema.py` | ★ 英譯陷阱（中譯英最容易錯的一格）：「計畫」是 plan（要畫什麼、依據什麼），**不是 schedule（時程）**——而 fields.TEXT_REGIONS 裡的 schedule 是「表格區／門窗表」又是第三個意思，三者不可混。⚠ 另一個撞字：本詞與「平面圖（floor plan）」在同一份文件都會大量出現，建議首次標成「繪圖計畫（drawing plan，即… |
| △★ | 脈絡節食 | context diet | `planning/context.py（docstring 首行即「§7.1 脈絡節食」）/ build_context()` | 本專案自創的比喻，沒有業界對應詞；直譯保住「節食」的味道，calque 語意透明、誤讀風險低。業界近似說法是 context budgeting／context pruning（可在 note 並陳讓工程背景讀者抓得到），但 repo 沒有對應識別碼，故以直譯為主，不建議替換。實質內容：中樞只收摘要（sheets 該列、品質地圖差區、排除帶、全部 chains、eleme… |
| △★ | 被動層 | passive layer | `perception/run_passive.py 的 run_passive()（v0.3 舊套件名即 passive/）` | 本專案自創，指 s01–s05 這段不做決策、只把事實抽出來、一次跑完、不迴圈的確定性階段。passive 這個字在專案內已由程式碼與歷史雙重定案。⚠ 同句的「確定性（deterministic）」第一次出現時宜一併標。第一個可標位置：v0.4 變更 3（第 40 行）。 |
| △★ | 視神經 | perception | `perception/（v0.4.1 變更 7 由 passive/ 改名）` | 本專案自創的比喻詞，沒有業界標準英文。★ 四組一致：CLAUDE.md 明言「套件用架構通用詞，中文比喻留在 docstring 與規格書」，標註時直接寫 perception，**不要標 optic nerve**（字面比喻只放 note）。perception → planning → execution 這組分層借自自駕與機器人領域，ADR 0001 決定段定案。⚠… |
| ⚠ | 主動工具箱 | active toolbox | `planning/tools.py 的 TOOL_SPECS（8 支）` | 本專案自創，「主動」是相對於 §7.1 被動收到的摘要——中樞要更多資訊得自己去拿，全部唯讀。★ 沒把握之處：active toolbox 非業界詞；實體就是 LLM tool calling，若讀者是工程背景，標 tool calling／tool set 比逐字的 active toolbox 好懂。★ 已更正清單數目：是 8 支不是 6 支（見「工具規格」條）。第一… |
| △ | 假中樞 | fake proposer | `planning/fake_proposer.py（v0.4.1 變更 8 由 fake_brain.py 改名）` | 回一份完整可跑的固定 plan，讓整個迴圈先驗通（§11 第 5 步刻意先於第 6 步：「真中樞接上時只剩一個變因」），與 proposer.propose_plan() 同簽章。★ **絕不要寫 fake brain**——那個字已退出 repo。★ 兩組提 fake planner（與「中樞→planning／the planner」一致），但識別碼是 fake_pr… |
| ⚠ | 契約三件套 | the three contract modules | `core/fields.py / core/classes.py / core/plan_schema.py` | 本專案自創，英文沒有既定說法。★ 三組給了三個譯法（the three contract modules／the contract triad／the three contract files），取最直白、最不像既有術語的 the three contract modules；備選 contract triad 讀來自然，可由負責人擇一寫死。⚠ 絕不要逐字譯 three-… |
| ⚠ | 存量配對盤點 | paired-sample inventory of the existing drawing stock | `CLAUDE.md §12 / docs 第 365、463 行 / perception/s03_detect.py / detectors/yolo.py` | ★ 語意未定，英文暫不採用。可確認的半邊：README 第 283 行「任何有大量紙本圖說存量、又要做數位轉換的地方」——「存量」＝既有紙本圖說的存量（existing stock）。未確認的半邊：「配對盤點」到底盤點什麼？看起來像是開 YOLO 前要先盤點「可配對的既有圖檔與標註量」，但四份文件都沒定義。★ 請負責人確認本意後再定英文，**不要逕譯**。出現 5 處，頻… |
| ⚠ | 思考史 | deliberation history | `plans/（plan_v1 → residuals_v1 → plan_v2 … 的序列）` | 本專案自創，沒有業界對應詞。★ 兩組給了不同譯法：deliberation history（較貼原意）／reasoning trail（較接近「可追溯的推理歷程」），另有 plan history／decision trail／thinking history。取 deliberation history，但★ 需負責人拍板——這個詞在 CLAUDE.md、cases/_… |
| ⚠ | 戒律 | commandment | `planning/prompts.py（戒律一：不准目測座標／戒律二：不准抹平矛盾／戒律三：不准改流程）` | 本專案自創、帶宗教色彩的比喻。commandment 保住那個味道，precept／prohibition／hard rule／cardinal rule 較平實但失味；沒有客觀依據可判哪個對，屬風格選擇，★ 建議由負責人定。「戒律一」= Commandment 1。⚠ 位置：§7.3 的 code block（325–332 行）與 planning/prompts.p… |
| △ | 未解釋粗線 | unexplained stroke | `fields.RESIDUAL_KINDS 的 unexplained_stroke（另兩種 low_coverage、over_reach）` | 指圖上有明顯粗線、但 plan 裡沒有任何 judgment 解釋它。stroke 此處指筆畫，與 classes.py wall 判準的「單一粗筆畫」同義。⚠ 英文丟掉了「粗」這個資訊（中文的粗線指足以構成牆的粗筆畫），但識別碼已定，不建議改成 unexplained thick stroke。可標位置：第 211 行。 |
| △ | 殘差偵探 | residual detective | `planning/subagents/detective.py / investigate(case_dir, residual_id) / 工具 ask_detective` | 本專案自創，但兩半都有背書（residual 是統計／測量標準詞，detective 由程式碼釘死）。★ 小更正：該檔的函式名是 investigate() 不是 detect／detective 開頭，文件裡提函式時別寫錯。「殘差」本身第一次出現時宜先標。第一個可標位置：v0.4 變更 2（第 39 行）。 |
| △ | 真中樞 | real proposer | `planning/proposer.py` | 與「假中樞」成對：接上真 LLM 的那一支（雲端走 ANTHROPIC_API_KEY／OPENAI_API_KEY，本地走 openai_compat）。備選 real planner／LLM-backed planner，但為與「假中樞 fake proposer」成對，取 real proposer。★ 同樣不可標 brain。 |
| ⚠ | 眼球轉動 | eye movement | `planning/tools.py 檔頭「§7.2 中樞主動工具箱（眼球轉動）」` | 本專案自創的比喻：中樞不是一次看完全圖，而是主動把視線移到要看的地方。★ 沒把握之處：saccade（跳視）是眼動研究的正式術語、語意其實最貼，但用在這裡過度學術，可能讓讀者以為在講認知科學文獻，故降到 note；直白的 eye movement 較安全。程式碼沒有對應識別碼（工具都叫 crop_look／redetect／measure…）。有一組建議此詞乾脆不標英文，亦可。 |
| △ | 神經系統 | nervous system |  | 本專案自創的整體比喻，統攝視神經／中樞／手三段。直譯即可，只需在 §2 標題標一次（第 108 行「## 2. 神經系統總覽」，標題行不在圍欄內，可標）。 |
| ⚠ | 腦手分離 | planning–execution separation | `s07_execute.py 檔頭「★ 腦手分離的實作保證」` | ★ 四組有實質衝突，取硬規則勝出的一方。有一組主張 brain-hand separation 是「全 repo 唯一還能用 brain 的位置（它是比喻不是模組名）」，但 ADR 0001／v0.4.1 變更 8 說的是「英文 brain 一詞**退出 repo**」，而 docs/ 就在 repo 裡——標 brain-hand separation 等於把已退場的詞… |
| △ | 讀數仲裁 | reading arbitration | `planning/subagents/arbiter.py / arbitrate(crop, readings, quality_zone) / 工具 ask_arbiter` | 本專案自創的 sub-agent 名稱，但兩半都有背書：arbiter／arbitration 由程式碼釘死，「讀數」對 reading（國教院有據）。★ 語意分工：指角色用 reading arbiter，指動作／這個機制用 reading arbitration——中文「讀數仲裁」是後者，故取 arbitration 為主詞。語意要點（§7.4）：裁不出來回 null… |
| △ | 跨樓層檢查 | cross-storey check | `review/s09_crosscheck.py / fields.CONFLICT_SOURCES 的 cross_storey` | ★ 衝突：一組主張 cross-floor（美式較好懂），但識別碼已釘死英式拼法 cross_storey（CONFLICT_SOURCE = CONFLICT_SOURCES[2]），中文標註取 cross-storey 保持一致，放棄 cross-floor／cross-story。⚠ 專案內 storey／story／floor 三種寫法並存是既有事實（樓層欄位另叫… |
| △ | 迴圈控制權 | control of the loop | `planning/orchestrator.py` | 取得程式碼佐證：orchestrator.py 檔頭自稱「全系統唯一的流程控制器」、明寫「for 迴圈、終止條件、退件，全在這支程式碼；中樞永遠只被呼叫」。英文慣用說法 the orchestrator owns the loop 可放心用；譯 loop control 亦可。⚠ 不要用 control flow（那是另一回事）。CLAUDE.md §12 現在不要做的第… |
| ⚠ | 順序即 id | order is the id | `core/classes.py（15 類清單順序即 class id，永不重排）` | ADR 0003 的標題用語。業界沒有對應成詞；positional id／ordinal id／index-as-id 都是描述而非定名，故英文只給字面直譯、不加括號補語，以免看起來像既有術語。後果：新類別只能 append 到 id 15 起（`calc_note` 是第一個追加，待負責人核可）。 |

---

★ 標記者為高頻詞，**每份文件只標第一次**，之後直接用中文 —— 每次都標會讓文章讀不下去。

## 刻意不收的詞（50）

一般中文詞、已經是英文或識別碼的、只出現一次的：

冪等 —— 判準「只出現一次且不重要」：已 grep 全 repo，只在 tests/smoke.py:193 一行程式註解出現，四份文件都沒用到。日後規格書用到再收（英文 idempotent）。、拼版 —— 全文只出現一次（規格第 445 行），且主譯無法定案：國教院「拼版」對到 composition（測繪）／stone（新聞傳播），page make-up 是電子計算機名詞、imposition 是印刷業的落版（把多頁排到大紙上，與本案「把掃描影像擺進 Illustrator 頁面」不同）。三個候選都不對，建議不標。、案號 —— 判準「檔名／欄位名不收」：cases/68-01782 是資料夾命名。要點明時直接寫 construction license number（見「建照」條）。、地段 —— 只在 cases/_template/README.md 出現一次，且本意未定：實例 `68-01782_某某街` 填的是街名（口語的「地點」義），不是地籍段名。土地法英譯把「段」譯 section、「地號」譯 plot＋serial number，但真地籍段名 land section 與地點標籤 locality/street 不可用同一個英文帶過。需負責人確認本意，暫不收。、圖面 —— 一般中文詞（drawing），且已由「子圖 drawing view」「圖種 drawing type」「版面 sheet layout」涵蓋。只需記住：泛指圖紙內容用 drawing、指單張圖幅用 sheet，兩者不可互換。、製圖 —— 一般中文詞，已折進「製圖慣例 drafting convention」的 note（drafting 是行為與職能、drawing 是圖說本身，絕不可譯 mapping／plotting）。、欄杆／欄板 —— classes.py 已裁定用 wall＋高度屬性表達、不另立類；且「欄板」英文無法查證（solid balustrade／solid parapet／solid guard 都見得到，WebSearch 額度用罄）。低頻＋無定論，暫不收。、選項 —— ADR 模板段名（MADR 作 Considered Options），行文中的「選項」是一般中文詞。只需在 docs/adr/README.md 說明模板段名一次，不必逐份標。、後果 —— 同上，Nygard ADR 四段（Context／Decision／Consequences／Status）之一，隨模板一次說明即可。、欄位 —— 一般技術詞（field／column），且 core/fields.py 檔名已是英文，依「已經是程式碼識別碼」判準不收。、相依／相依套件 —— 一般技術詞（dependency）。只保留「硬相依 hard dependency」，因為它與 optional dependency 的對立在 §8.1 有實質作用。、版控 —— 一般技術詞（version control）。附帶提醒：「不進版控」的英文慣說法是 not tracked／gitignored，不是 not in version control 的逐字譯。、入口 —— 一般技術詞（entry point）。、下游 —— 一般技術詞（downstream）。、唯讀 —— 一般技術詞（read-only）。、單輪 —— 一般技術詞（single-turn，對 multi-turn）。、多模態 —— 一般技術詞（multimodal），目標讀者不需要這個括號。、金鑰 —— 一般技術詞（API key）；語意重點在「不進 core/config.py」，已由「個資 personal data」條承載。、工具呼叫 —— 一般技術詞（tool call；Anthropic 文件作 tool use、OpenAI 作 function calling）。已由「工具規格 tool spec」「主動工具箱 active toolbox」涵蓋。、寫死 —— 一般技術詞（hard-code）。文件裡另有同義詞「硬編」，兩處同一個英文，都不必標。、公開介面 —— 一般技術詞（public interface／public API）。、抽象層 —— 一般技術詞（abstraction layer）。、擴充點 —— 一般技術詞（extension point），已折進「投機性抽象」的 note。、設計模式 —— 一般技術詞（design pattern）。具體的 Adapter 已由「轉接器」條承載。、重構 —— 一般技術詞（refactoring）。、心智模型 —— 一般中文詞組（mental model），只在 ADR 0002 出現一次。、區域變數遮蔽 —— 只在 ADR 0001 出現一次，且屬 Python 語言常識（local variable shadowing；不要譯 masking／hiding）。、補集 —— 數學一般詞（complement）。其論證價值已折進「詞庫 lexicon」的 note。、命名規約 —— 一般技術詞（naming convention）。具體規約（*_line 是幾何、*_text 走三源）已折進「尺寸數字」條。、管線 —— 一般技術詞（pipeline），已折進「確定性 determinism」的 note（deterministic pipeline）。、生命週期 —— 一般技術詞（lifecycle），已折進「富標籤」條（status lifecycle: proposed → adopted/rejected/uncertain）。、工具鏈 —— 一般技術詞（toolchain），且 build/lint/test 工具鏈目前尚未建立。、規格書 —— 一般中文詞（specification／spec）。全檔簡稱「規格」，條號引用寫 §N 即可。、門檻 —— 一般技術詞（threshold），且 thresholding 已由「自適應二值化」條承載。、供應商介面 —— 由「供應商 provider」＋「薄介面 thin interface」兩條組成（provider interface），不另收。、校對介面 —— 由「校對 review」條涵蓋（review interface），不另收。、代擬 —— 描述性英譯（drafted on the owner's behalf／draft-and-review），不是 term of art，硬標一個括號沒有意義；建議把整句改寫成英文說得通的說法。（ghost-drafting 語感不對，偏代筆掛名，不要用。）、落地 —— 中文技術圈口語，英文無單詞對應，必須看語境改寫整句：「唯二有落地產出的類」→ the only two classes that produce real downstream output；「CAD 落地路線盤查」→ CAD delivery routes。不適合做成一對一詞條。、設計面積 —— 中英兩邊都在自創（design surface area 不是既定英文詞組，「設計面積」也不是既定中文術語）。建議把整句改寫掉（the design covers more surface than the need），不加括號。、指令包 —— 只在 ADR 0006 標題出現一次，無既定英文（instruction bundle／prompt pack 皆為描述性譯法）。低頻＋無定論，不收。、參數化 —— 已由「參數化物件 parametric object」涵蓋，單用時直接寫 parametric。、特徵點拼接 —— 已由「拼接 image stitching」涵蓋（feature-based image stitching），s01_ingest 明令不做，只需在該處一次說明。、粗胚（單用）—— 已由「粗胚 BIM（draft BIM）」涵蓋。單用形容 _draft 後綴時只寫 rough／unfinished，⚠ 絕不要套 bare shell／shell-and-core／scratch coat 等營建術語，那會讓讀者誤以為在講施工階段。、驗收閘門 —— 由「驗收條件 acceptance criteria」＋「閘門 gate」組成（acceptance gate），已寫進兩條的 note。CI 領域更常見的既有詞是 quality gate。、三源讀取 —— 由「三源 three sources」涵蓋（three-source reading）。、品質區 —— 欄位名 quality_zone，依「欄位名不收」判準排除；已在「品質地圖」條註明要與地圖分開。、目測（單用）—— 由「目測座標 eyeballed coordinates」涵蓋。國教院的 visual observation／ocular estimation 語意是「觀測」不是「估算」，不採。、存量（單用）—— 由「存量配對盤點」條涵蓋（existing drawing stock）。、建照圖 —— 由「建照 construction license」條涵蓋（permit drawing set／permit set）。、矩形／實心矩形、封閉小矩形等偵測描述 —— 一般中文詞組，非術語。


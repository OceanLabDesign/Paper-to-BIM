# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.venv-build/

# PyInstaller 的產物（一次 333MB：dist/ 同時有 .app 與 COLLECT 目錄）
build/
dist/
*.spec.bak

# macOS
.DS_Store

# Claude Code 本機設定（每台機器不同，不進版控）
.claude/settings.local.json

# ── 案件資料 ─────────────────────────────────────────────
# 掃描圖、中間產物、輸出一律不進版控：
#   00_raw 的原始 PDF 可能含個資與著作權；
#   tiles / crops / DXF / CSV 體積大，而且照定義都能從 00_raw 重生。
# 只保留 §3 的資料夾骨架（.gitkeep）。
cases/**
!cases/**/
!cases/**/.gitkeep
!cases/_template/README.md

# 內部決策/溝通文件：留在本機，不進版控
待決事項.md
待決事項_裁決.md

# 掃描檔一律不進版控 —— 不論放在哪裡。
# ★ 2026-08-22 踩過：檔案放在根目錄時 cases/** 擋不到，被 git add -A 掃進去並推上公開 repo。
*.pdf
*.PDF
*.tif
*.tiff

# ── 安裝程式（install.py）的產物 ─────────────────────────
# 都是本機產生的，跟這台機器的路徑與 Python 綁在一起，不該進版控。
# ⚠ 註解只能獨立成行 —— gitignore 只認**行首**的 #，
#   寫在 pattern 後面會讓整串（含空白與註解文字）變成檔名的一部分。
# 縮圖快取（案件資料夾內，重跑會重建）
.thumbs/
# 啟動器：macOS 的 .command 與 .app 外殼、Windows 的 .bat
Paper-to-BIM.command
Paper-to-BIM.bat
Paper-to-BIM.app/

# ── 不屬於本專案的檔案 ───────────────────────────────────
# ★ 同 *.pdf 那條的教訓：放在根目錄的東西 cases/** 擋不到，
#   會被 git add -A 掃進公開 repo。無關的檔案就明確擋掉。
farside_brand_assets_v2.html
